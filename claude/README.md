# claude/

Claude Code-specific skills. These use features that only exist in the Claude
Code runtime: `context: fork`, hooks, `.claude/` directory conventions, or
`claude --permission-mode bypassPermissions` patterns.

Skills here will not work as intended in OpenClaw or other agents.
